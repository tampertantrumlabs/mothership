# Per-developer Claude Code settings (shared settings go in .claude/settings.json)
.claude/settings.local.json

# OS + editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Python (for when runtime work lands)
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.env
.env.local
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node (for tooling)
node_modules/

# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Local artifacts
*.log
*.tmp

# Build outputs that shouldn't be committed
/dist
/build
